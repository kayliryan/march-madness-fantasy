/**
 * The most recently completed tournament season — displayed as the
 * current/active season, not historical. Leagues with
 * `season < CURRENT_TOURNAMENT_SEASON` are historical (fully-played
 * past seasons) and rendered read-only throughout the UI.
 */
export const CURRENT_TOURNAMENT_SEASON = 2026;
